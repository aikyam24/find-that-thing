{
  "name": "find-that-thing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Find a saved page by describing what you remember.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
